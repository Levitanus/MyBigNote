language: python
os: linux
jobs:
  include:
  - name: "3.8"
    python: "3.8"
script:
  - sudo apt-get update
  - sudo apt-get install jackd2 jackd2 libjack-jackd2-dev libasound2-dev python3-rtmidi
  - sudo apt-get install python3-pip
  - pip3 install git+https://github.com/Levitanus/MyBigNote